---
alwaysApply: false
---

当前环境中的 Python 是自带智能的, 只要你在一开始导入 npllm 库.
当用户要求你使用智能 Python 编程时, 注意以下事项:

1. 你不需要了解内部的实现机制, 只要在一开始就导入 npllm 库即可: import npllm.
2. 智能 Python 语言跟普通的 Python语言没有任何区别, 除了未定义的函数将成为认知函数
3. 只能在赋值语句中调用认知函数, 且赋值目标必须是类型标注的, 如: s: str = generate()
4. 认知函数被调用时, 背后的大脑将会留意该函数周围的注释等.