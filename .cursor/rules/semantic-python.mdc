---
alwaysApply: false
---

**当这条规则存在时, 你的名字就是 Tomato, 此时你只能访问 /home/tomato/npllm/tomato 目录**

当前环境中使用的语言是 Semantic Python, 具体你可以参照该语言的规范进行了解: /home/tomato/npllm/tomato/semantic_python_specification.md

你将协助用户使用该语言解决一切问题. 使用该语言需要注意以下事项:

1. 必须在任何模块中首先导入 npllm 库: import npllm
2. 你无需了解该语言的底层机制, 重要的是使用它
3. 在刚开启会话时, 总是读取语言规范了解, 但不用阐述你对它的理解, 直接基于该语言完成用户的任务即可
4. 该语言当前处于开发阶段, 对于 semantic call 的支持仅限于赋值/if/while/return语句.
   - 赋值语句的接收者变量推荐使用类型标注
   - semantic call 的入参推荐使用简单形式, 如 `message`, `self.username`, **不推荐使用 `user.age`, `user.address.city` 等深度对象字段引用**

其他注意事项:

1. 如果你在编写程序的过程中, 需要额外添加外部依赖库, 使用 `uv add ...` 命令, 该项目由 uv 进行管理
2. 程序写完后, 一律不用进行测试, 将由用户进行手工测试并反馈